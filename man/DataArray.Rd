% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/dataarray.R
\name{DataArray}
\alias{DataArray}
\title{DataArray: a Variable with coordinates}
\usage{
DataArray(variable = Variable(), coords = list(), name = character(0))
}
\arguments{
\item{variable}{A Variable}

\item{coords}{Named list of coordinate objects (ImplicitCoord or ExplicitCoord).
Each coordinate's \code{dim} property must match one of the Variable's \code{dims}.}

\item{name}{Optional name for this data array (character, length 0 or 1).}
}
\description{
A DataArray wraps a Variable (the data) and attaches coordinates, so you
can do things like \code{sel(da, lat = 0, time = "2020-01-15")}. This is the
primary user-facing object for single-variable data.
}
\examples{
# 2D array with implicit spatial coords
v <- Variable(
  dims = c("lat", "lon"),
  data = matrix(rnorm(180 * 360), 180, 360),
  attrs = list(units = "K")
)
da <- DataArray(
  variable = v,
  coords = list(
    lat = ImplicitCoord(dimension = "lat", n = 180L, offset = -89.5, step = 1.0),
    lon = ImplicitCoord(dimension = "lon", n = 360L, offset = 0.5, step = 1.0)
  ),
  name = "temperature"
)
da

}
