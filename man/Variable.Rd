% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/variable.R
\name{Variable}
\alias{Variable}
\title{Variable: a named-dimension array}
\usage{
Variable(dims = character(0), data = NULL, attrs = list(), encoding = list())
}
\arguments{
\item{dims}{Character vector of dimension names.}

\item{data}{An R array (or matrix, or vector with dim attribute).}

\item{attrs}{Named list of arbitrary metadata.}

\item{encoding}{Named list of on-disk encoding info (scale_factor, etc.).}
}
\description{
The fundamental building block. A Variable is an N-dimensional array that
knows the names of its dimensions. It does NOT know about coordinates —
that's DataArray's job.
}
\examples{
# A 3D temperature field
temp_data <- array(rnorm(365 * 180 * 360), dim = c(365, 180, 360))
v <- Variable(
  dims = c("time", "lat", "lon"),
  data = temp_data,
  attrs = list(units = "K", long_name = "Temperature")
)
v

# Scalar variable
s <- Variable(dims = character(), data = array(42))
s

}
