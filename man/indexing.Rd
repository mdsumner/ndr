% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/sel.R
\name{indexing}
\alias{indexing}
\alias{isel}
\alias{sel}
\title{Coordinate-based indexing}
\usage{
isel(x, ...)

sel(x, ...)
}
\arguments{
\item{x}{A Variable, DataArray, or Dataset}

\item{...}{Named arguments specifying the selection. For \code{sel()}, values
are coordinate values. For \code{isel()}, values are integer indices (1-based).
Use a vector for ranges: \code{sel(da, lat = c(-30, 30))} selects the range.
Use a scalar for a single position: \code{isel(da, time = 1)} picks one index
and drops that dimension.}
}
\value{
Same type as input, with dimensions sliced or dropped.
}
\description{
\code{sel()} selects data by coordinate value (label-based).
\code{isel()} selects data by integer index (position-based).
}
\details{
Both work on Variables, DataArrays, and Datasets, and return objects of
the same type with dimensions correctly updated.
}
\examples{
v <- Variable(
  dims = c("lat", "lon"),
  data = matrix(1:12, 3, 4)
)
da <- DataArray(
  variable = v,
  coords = list(
    lat = ImplicitCoord(dimension = "lat", n = 3L, offset = -10, step = 10),
    lon = ImplicitCoord(dimension = "lon", n = 4L, offset = 100, step = 10)
  )
)

# Select by coordinate value
sel(da, lat = 0)             # single latitude, drops lat dim
sel(da, lat = c(-10, 0))     # range of latitudes

# Select by integer index
isel(da, lon = 1:2)          # first two longitude columns
isel(da, lat = 2, lon = 3)   # single cell

}
